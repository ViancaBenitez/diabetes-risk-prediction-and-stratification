# -*- coding: utf-8 -*-
"""Diabetes_riskprediciton_app

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1LWXOGhrDAJbrLwcUpwZ6Lum_qaiUPsy_
"""

# =========================
# IMPORTS
# =========================
import streamlit as st
import joblib
import numpy as np
from datetime import date
import matplotlib.pyplot as plt

# =========================
# LOAD MODEL & SCALER
# =========================
model = joblib.load("model.pkl")
scaler = joblib.load("scaler.pkl")

# =========================
# FUNCTIONS
# =========================
def get_risk_level(prob):
    if prob < 0.3:
        return "Low Risk"
    elif prob < 0.7:
        return "Medium Risk"
    else:
        return "High Risk"

def get_recommendation(risk):
    if risk == "High Risk":
        return "Consider insulin therapy (clinical decision required)"
    elif risk == "Medium Risk":
        return "Oral medication + lifestyle modification"
    else:
        return "Lifestyle monitoring"

def get_bmi_category(bmi):
    if bmi < 18.5:
        return "Underweight"
    elif bmi < 23:
        return "Normal Weight"
    elif bmi < 25:
        return "Overweight"
    elif bmi < 30:
        return "Obese Class I"
    else:
        return "Obese Class II"

def get_glucose_category(glucose):
    if glucose < 140:
        return "Normal"
    elif glucose < 200:
        return "Prediabetes"
    else:
        return "Diabetes"

def get_dpf_category(dpf):
    if dpf < 0.3:
        return "Low Genetic Risk"
    elif dpf < 0.7:
        return "Moderate Genetic Risk"
    else:
        return "High Genetic Risk"

# =========================
# PAGE CONFIG
# =========================
st.set_page_config(page_title="Diabetes CDSS", layout="wide")

# =========================
# HEADER
# =========================
st.title("🩺 Diabetes Clinical Decision Support System")
st.markdown("### AI-Powered Risk Prediction, Stratification, and Treatment Guidance")
st.divider()

# =========================
# PATIENT DETAILS
# =========================
st.subheader("👤 Patient Details")

colA, colB, colC = st.columns(3)

with colA:
    name = st.text_input("Patient Name")

with colB:
    sex = st.selectbox("Sex", ["Female", "Male"])

with colC:
    visit_date = st.date_input("Date", value=date.today())

# =========================
# CLINICAL NOTES
# =========================
st.info("""
🏥 Clinical Notes:

• BMI uses Asia-Pacific (Filipino) classification
• Glucose is measured in mg/dL
• BP uses diastolic value only (dataset limitation)
• Insulin = 0 may indicate missing data
• DPF = genetic risk score from family history
• Sex is not used in prediction
""")

# =========================
# INPUT SECTION
# =========================
st.subheader("🧾 Patient Information")

col1, col2, col3 = st.columns(3)

with col1:
    preg = st.number_input("Pregnancies", 0, 20)
    glucose = st.number_input("Glucose (mg/dL)", 0, 300)
    bp = st.number_input("Blood Pressure (Diastolic)", 0, 150)

with col2:
    skin = st.number_input("Skin Thickness", 0, 100)
    insulin = st.number_input("Insulin", 0, 900)
    bmi = st.number_input("BMI", 0.0, 70.0)

with col3:
    dpf = st.number_input("Diabetes Pedigree Function", 0.0, 3.0)
    age = st.number_input("Age", 1, 120)

st.divider()

# =========================
# PREDICT BUTTON
# =========================
if st.button("🔍 Analyze Patient Risk", use_container_width=True):

    input_data = np.array([[preg, glucose, bp, skin, insulin, bmi, dpf, age]])
    input_scaled = scaler.transform(input_data)

    pred = model.predict(input_scaled)[0]
    prob = model.predict_proba(input_scaled)[0][1]

    risk = get_risk_level(prob)
    reco = get_recommendation(risk)

    # =========================
    # RESULTS
    # =========================
    st.subheader(f"📊 Results for {name}" if name else "📊 Clinical Assessment Results")

    colX, colY, colZ = st.columns(3)

    with colX:
        st.markdown("#### 🧪 Diagnosis")
        if pred == 1:
            st.error("Positive (Diabetes Detected)")
        else:
            st.success("Negative (No Diabetes Detected)")

    with colY:
        st.markdown("#### 📈 Probability")
        st.metric("Diabetes Likelihood", f"{prob:.2f}")

    with colZ:
        st.markdown("#### ⚠️ Risk Level")
        if risk == "High Risk":
            st.error("🔴 High Risk")
        elif risk == "Medium Risk":
            st.warning("🟡 Medium Risk")
        else:
            st.success("🟢 Low Risk")

    st.divider()

    # =========================
    # 📊 RISK CHART
    # =========================
    st.subheader("📊 Risk Visualization")

    fig, ax = plt.subplots()
    ax.barh(["Risk"], [prob])
    ax.set_xlim(0, 1)
    ax.set_xlabel("Probability")
    ax.set_title("Diabetes Risk Score")

    st.pyplot(fig)

    st.divider()

    # =========================
    # INTERPRETATION
    # =========================
    st.subheader("📌 Clinical Interpretation")

    bmi_cat = get_bmi_category(bmi)
    glucose_cat = get_glucose_category(glucose)
    dpf_cat = get_dpf_category(dpf)

    st.write(f"**BMI Category (PH):** {bmi_cat}")
    st.write(f"**Glucose Category:** {glucose_cat}")
    st.write(f"**Genetic Risk (DPF):** {dpf_cat}")

    st.info(
        f"The patient is classified as **{risk}** based on model prediction. "
        f"This supports (not replaces) clinical judgment."
    )

    # =========================
    # RECOMMENDATION
    # =========================
    st.subheader("💊 Recommended Intervention")

    if risk == "High Risk":
        st.error(reco)
    elif risk == "Medium Risk":
        st.warning(reco)
    else:
        st.success(reco)

    st.divider()

    # =========================
    # REPORT
    # =========================
    st.subheader("🧾 Patient Report")

    report_text = f"""
Patient Name: {name if name else 'N/A'}
Sex: {sex}
Date: {visit_date}

--- INPUT SUMMARY ---
Pregnancies: {preg}
Glucose: {glucose} mg/dL ({glucose_cat})
Blood Pressure: {bp} mm Hg
Skin Thickness: {skin} mm
Insulin: {insulin}
BMI: {bmi:.2f} ({bmi_cat})
DPF: {dpf} ({dpf_cat})
Age: {age}

--- RESULTS ---
Prediction: {"Positive (Diabetes)" if pred == 1 else "Negative"}
Probability: {prob:.2f}
Risk Level: {risk}

--- RECOMMENDATION ---
{reco}

Note: This system is for decision support only.
"""

    st.text_area("Report Preview", report_text, height=300)

    st.download_button(
        label="⬇️ Download Report",
        data=report_text,
        file_name=f"diabetes_report_{name if name else 'patient'}.txt",
        mime="text/plain"
    )

    st.divider()
    st.caption("⚠️ This system is for decision support only and does not replace clinical judgment.")